services:
  atlas-trader:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: atlas-trade-engine
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    command: ["--mode", "paper"]
